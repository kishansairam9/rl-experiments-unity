using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * Creates a Randomly generated map for the payload manipulation task.
 * The map involves generating rooms all around the arena with some
 * entrance such that the payload can be manipulated through those entrances.
 */
public class CreateMap : MonoBehaviour
{
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
